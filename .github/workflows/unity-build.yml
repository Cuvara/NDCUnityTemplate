name: Unity Build

# Explicit per-platform Unity build for NDC-Unity-Template.
#
# Each platform appears as a distinct job in the GitHub Actions UI.
# All build logic is delegated to toolkit reusable workflows:
#   - reusable-build-platform.yml  (Android / WebGL / Linux64 / LinuxServer / iOS / Addressables)
#   - reusable-unity-tests.yml     (EditMode / PlayMode / All)
#
# Architecture: see unity-build-workflows/docs/EXPLICIT_PLATFORM_FLOW_SPEC.md
# Activation:   see unity-build-workflows/docs/UNITY_PERSONAL_DOCKER_LICENSE.md

on:
  workflow_dispatch:
    inputs:
      platform:
        description: "Target platform(s)"
        required: false
        default: All
        type: choice
        options:
          - All
          - Android
          - WebGL
          - Linux64
          - LinuxServer
          - iOS

      environment:
        description: "Build environment (passed to Unity)"
        required: false
        default: production
        type: choice
        options:
          - production
          - staging
          - development

      run-tests:
        description: "Run EditMode/PlayMode tests before building"
        required: false
        default: false
        type: boolean

      test-mode:
        description: "Test suite to run (only when run-tests=true)"
        required: false
        default: All
        type: choice
        options:
          - EditMode
          - PlayMode
          - All

      build-addressables:
        description: "Run Addressable build pipeline before platform builds"
        required: false
        default: false
        type: boolean

      activation-strategy:
        description: "Unity license activation strategy (docker lane only)"
        required: false
        default: auto
        type: choice
        options:
          - auto
          - manual-license
          - account
          - serial
          - preactivated
          - none

      runner-mode:
        description: "Execution lane"
        required: false
        default: docker
        type: choice
        options:
          - docker
          - self-hosted-windows
          - auto

      unity-version:
        description: "Override Unity version (blank = use ProjectSettings/ProjectVersion.txt)"
        required: false
        default: ''
        type: string

      clean-build:
        description: "Force clean Library cache before building (slow)"
        required: false
        default: false
        type: boolean

# ── Concurrency: cancel stale runs on the same branch/platform ──────────────
concurrency:
  group: unity-build-${{ github.ref }}-${{ inputs.platform }}
  cancel-in-progress: true

jobs:

  # ════════════════════════════════════════════════════════════════════════════
  # JOB 1: resolve-config
  #   Reads ProjectVersion.txt (SSOT) and emits resolved unity-version,
  #   project-path, and toolkit-ref for all downstream jobs.
  # ════════════════════════════════════════════════════════════════════════════
  resolve-config:
    name: Resolve Config
    runs-on: ubuntu-latest
    outputs:
      unity-version: ${{ steps.version.outputs.version }}
      project-path:  ${{ steps.version.outputs.project-path }}
      toolkit-ref:   ${{ steps.version.outputs.toolkit-ref }}
    steps:
      - name: Checkout (shallow — only needs ProjectVersion.txt)
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            ProjectSettings/ProjectVersion.txt
          sparse-checkout-cone-mode: false

      - name: Resolve Unity version & config
        id: version
        shell: bash
        env:
          OVERRIDE: ${{ inputs.unity-version }}
          PROJECT_PATH: '.'
          TOOLKIT_REF: 'feature/explicit-platform-jobs'
        run: |
          set -euo pipefail
          PV="${PROJECT_PATH%/}/ProjectSettings/ProjectVersion.txt"
          if [ ! -f "$PV" ]; then
            echo "::error::ProjectVersion.txt not found at ${PV}"
            exit 1
          fi
          PROJECT_VERSION=$(grep '^m_EditorVersion:' "$PV" | awk '{print $2}')
          echo "Project Unity version (SSOT): ${PROJECT_VERSION}"
          if [ -n "${OVERRIDE}" ] && [ "${OVERRIDE}" != "${PROJECT_VERSION}" ]; then
            echo "::error::Unity version mismatch:"
            echo "::error::  ProjectVersion.txt: ${PROJECT_VERSION}"
            echo "::error::  Input override:     ${OVERRIDE}"
            echo "::error::Update ProjectVersion.txt to change the version (it is the SSOT)."
            exit 1
          fi
          RESOLVED="${OVERRIDE:-${PROJECT_VERSION}}"
          echo "version=${RESOLVED}"                          >> "$GITHUB_OUTPUT"
          echo "project-path=${PROJECT_PATH}"                 >> "$GITHUB_OUTPUT"
          echo "toolkit-ref=${TOOLKIT_REF}"                   >> "$GITHUB_OUTPUT"
          echo "Resolved: version=${RESOLVED} project-path=${PROJECT_PATH} toolkit-ref=${TOOLKIT_REF}"

  # ════════════════════════════════════════════════════════════════════════════
  # JOB 2: validate-project
  #   Verifies the Unity project structure is intact before spending build time.
  # ════════════════════════════════════════════════════════════════════════════
  validate-project:
    name: Validate Project
    needs: [resolve-config]
    if: needs.resolve-config.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate project structure
        shell: bash
        env:
          PROJECT_PATH: ${{ needs.resolve-config.outputs.project-path }}
          UNITY_VERSION: ${{ needs.resolve-config.outputs.unity-version }}
        run: |
          set -euo pipefail
          ERRORS=0

          check() {
            local path="$1" label="$2"
            if [ ! -e "${PROJECT_PATH%/}/${path}" ]; then
              echo "::error::Missing ${label}: ${PROJECT_PATH%/}/${path}"
              ERRORS=$((ERRORS + 1))
            else
              echo "  ✓ ${label}"
            fi
          }

          echo "Validating Unity project at '${PROJECT_PATH}' (Unity ${UNITY_VERSION})"
          check "ProjectSettings/ProjectVersion.txt" "ProjectVersion.txt"
          check "Packages/manifest.json"             "Packages/manifest.json"
          check "Assets"                             "Assets directory"

          if [ "${ERRORS}" -gt 0 ]; then
            echo "::error::Project validation failed (${ERRORS} error(s))"
            exit 1
          fi
          echo "Project validation passed."

  # ════════════════════════════════════════════════════════════════════════════
  # JOB 3: unity-tests
  #   Runs EditMode / PlayMode / All tests.
  #   Skipped unless run-tests=true.
  #   Does NOT gate the build jobs — tests and builds are independent siblings.
  # ════════════════════════════════════════════════════════════════════════════
  unity-tests:
    name: Unity Tests (${{ inputs.test-mode }})
    needs: [resolve-config, validate-project]
    if: >
      inputs.run-tests == true &&
      needs.validate-project.result == 'success'
    uses: dyCuong03/unity-build-workflows/.github/workflows/reusable-unity-tests.yml@feature/explicit-platform-jobs
    with:
      unity-version:       ${{ needs.resolve-config.outputs.unity-version }}
      test-mode:           ${{ inputs.test-mode }}
      project-path:        ${{ needs.resolve-config.outputs.project-path }}
      runner-mode:         ${{ inputs.runner-mode }}
      activation-strategy: ${{ inputs.activation-strategy }}
      toolkit-ref:         ${{ needs.resolve-config.outputs.toolkit-ref }}
    secrets:
      UNITY_LICENSE:  ${{ secrets.UNITY_LICENSE }}
      UNITY_EMAIL:    ${{ secrets.UNITY_EMAIL }}
      UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}

  # ════════════════════════════════════════════════════════════════════════════
  # JOB 4: build-addressables
  #   Pre-builds the Addressable catalog and bundles.
  #   Platform builds depend on this job (success OR skipped).
  #   Skipped unless build-addressables=true.
  # ════════════════════════════════════════════════════════════════════════════
  build-addressables:
    name: Build Addressables
    needs: [resolve-config, validate-project]
    if: >
      inputs.build-addressables == true &&
      needs.validate-project.result == 'success'
    uses: dyCuong03/unity-build-workflows/.github/workflows/reusable-build-platform.yml@feature/explicit-platform-jobs
    with:
      platform:            Addressables
      unity-version:       ${{ needs.resolve-config.outputs.unity-version }}
      project-path:        ${{ needs.resolve-config.outputs.project-path }}
      environment:         ${{ inputs.environment }}
      runner-mode:         ${{ inputs.runner-mode }}
      activation-strategy: ${{ inputs.activation-strategy }}
      clean-build:         ${{ inputs.clean-build }}
      build-addressables:  false
      toolkit-ref:         ${{ needs.resolve-config.outputs.toolkit-ref }}
    secrets:
      UNITY_LICENSE:  ${{ secrets.UNITY_LICENSE }}
      UNITY_EMAIL:    ${{ secrets.UNITY_EMAIL }}
      UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}

  # ════════════════════════════════════════════════════════════════════════════
  # JOB 5: build-android
  # ════════════════════════════════════════════════════════════════════════════
  build-android:
    name: Build Android
    needs: [resolve-config, validate-project, build-addressables]
    if: >
      needs.validate-project.result == 'success' &&
      (needs.build-addressables.result == 'success' || needs.build-addressables.result == 'skipped') &&
      (inputs.platform == 'All' || inputs.platform == 'Android')
    uses: dyCuong03/unity-build-workflows/.github/workflows/reusable-build-platform.yml@feature/explicit-platform-jobs
    with:
      platform:            Android
      unity-version:       ${{ needs.resolve-config.outputs.unity-version }}
      project-path:        ${{ needs.resolve-config.outputs.project-path }}
      environment:         ${{ inputs.environment }}
      runner-mode:         ${{ inputs.runner-mode }}
      activation-strategy: ${{ inputs.activation-strategy }}
      clean-build:         ${{ inputs.clean-build }}
      build-addressables:  false
      toolkit-ref:         ${{ needs.resolve-config.outputs.toolkit-ref }}
    secrets:
      UNITY_LICENSE:  ${{ secrets.UNITY_LICENSE }}
      UNITY_EMAIL:    ${{ secrets.UNITY_EMAIL }}
      UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}

  # ════════════════════════════════════════════════════════════════════════════
  # JOB 6: build-webgl
  # ════════════════════════════════════════════════════════════════════════════
  build-webgl:
    name: Build WebGL
    needs: [resolve-config, validate-project, build-addressables]
    if: >
      needs.validate-project.result == 'success' &&
      (needs.build-addressables.result == 'success' || needs.build-addressables.result == 'skipped') &&
      (inputs.platform == 'All' || inputs.platform == 'WebGL')
    uses: dyCuong03/unity-build-workflows/.github/workflows/reusable-build-platform.yml@feature/explicit-platform-jobs
    with:
      platform:            WebGL
      unity-version:       ${{ needs.resolve-config.outputs.unity-version }}
      project-path:        ${{ needs.resolve-config.outputs.project-path }}
      environment:         ${{ inputs.environment }}
      runner-mode:         ${{ inputs.runner-mode }}
      activation-strategy: ${{ inputs.activation-strategy }}
      clean-build:         ${{ inputs.clean-build }}
      build-addressables:  false
      toolkit-ref:         ${{ needs.resolve-config.outputs.toolkit-ref }}
    secrets:
      UNITY_LICENSE:  ${{ secrets.UNITY_LICENSE }}
      UNITY_EMAIL:    ${{ secrets.UNITY_EMAIL }}
      UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}

  # ════════════════════════════════════════════════════════════════════════════
  # JOB 7: build-linux64
  # ════════════════════════════════════════════════════════════════════════════
  build-linux64:
    name: Build Linux64
    needs: [resolve-config, validate-project, build-addressables]
    if: >
      needs.validate-project.result == 'success' &&
      (needs.build-addressables.result == 'success' || needs.build-addressables.result == 'skipped') &&
      (inputs.platform == 'All' || inputs.platform == 'Linux64')
    uses: dyCuong03/unity-build-workflows/.github/workflows/reusable-build-platform.yml@feature/explicit-platform-jobs
    with:
      platform:            Linux64
      unity-version:       ${{ needs.resolve-config.outputs.unity-version }}
      project-path:        ${{ needs.resolve-config.outputs.project-path }}
      environment:         ${{ inputs.environment }}
      runner-mode:         ${{ inputs.runner-mode }}
      activation-strategy: ${{ inputs.activation-strategy }}
      clean-build:         ${{ inputs.clean-build }}
      build-addressables:  false
      toolkit-ref:         ${{ needs.resolve-config.outputs.toolkit-ref }}
    secrets:
      UNITY_LICENSE:  ${{ secrets.UNITY_LICENSE }}
      UNITY_EMAIL:    ${{ secrets.UNITY_EMAIL }}
      UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}

  # ════════════════════════════════════════════════════════════════════════════
  # JOB 8: build-linuxserver
  # ════════════════════════════════════════════════════════════════════════════
  build-linuxserver:
    name: Build LinuxServer
    needs: [resolve-config, validate-project, build-addressables]
    if: >
      needs.validate-project.result == 'success' &&
      (needs.build-addressables.result == 'success' || needs.build-addressables.result == 'skipped') &&
      (inputs.platform == 'All' || inputs.platform == 'LinuxServer')
    uses: dyCuong03/unity-build-workflows/.github/workflows/reusable-build-platform.yml@feature/explicit-platform-jobs
    with:
      platform:            LinuxServer
      unity-version:       ${{ needs.resolve-config.outputs.unity-version }}
      project-path:        ${{ needs.resolve-config.outputs.project-path }}
      environment:         ${{ inputs.environment }}
      runner-mode:         ${{ inputs.runner-mode }}
      activation-strategy: ${{ inputs.activation-strategy }}
      clean-build:         ${{ inputs.clean-build }}
      build-addressables:  false
      toolkit-ref:         ${{ needs.resolve-config.outputs.toolkit-ref }}
    secrets:
      UNITY_LICENSE:  ${{ secrets.UNITY_LICENSE }}
      UNITY_EMAIL:    ${{ secrets.UNITY_EMAIL }}
      UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}

  # ════════════════════════════════════════════════════════════════════════════
  # JOB 9: build-ios
  #   BLOCKED until a self-hosted macOS runner is provisioned.
  #   runner-mode is HARDCODED to self-hosted-macos regardless of dispatch input.
  #   If no macOS runner is registered, the reusable sets result=blocked and
  #   exits 0 (clean skip) — it never fake-passes.
  # ════════════════════════════════════════════════════════════════════════════
  build-ios:
    name: Build iOS
    needs: [resolve-config, validate-project, build-addressables]
    if: >
      needs.validate-project.result == 'success' &&
      (needs.build-addressables.result == 'success' || needs.build-addressables.result == 'skipped') &&
      (inputs.platform == 'All' || inputs.platform == 'iOS')
    uses: dyCuong03/unity-build-workflows/.github/workflows/reusable-build-platform.yml@feature/explicit-platform-jobs
    with:
      platform:            iOS
      unity-version:       ${{ needs.resolve-config.outputs.unity-version }}
      project-path:        ${{ needs.resolve-config.outputs.project-path }}
      environment:         ${{ inputs.environment }}
      # iOS ALWAYS uses self-hosted-macos — never docker or self-hosted-windows.
      runner-mode:         self-hosted-macos
      # macOS runner has local Unity already activated — no license secrets needed.
      activation-strategy: preactivated
      clean-build:         ${{ inputs.clean-build }}
      build-addressables:  false
      toolkit-ref:         ${{ needs.resolve-config.outputs.toolkit-ref }}
    secrets: inherit

  # ════════════════════════════════════════════════════════════════════════════
  # JOB 10: final-report
  #   Always runs. Summarises all job results in the workflow step summary.
  # ════════════════════════════════════════════════════════════════════════════
  final-report:
    name: Final Report
    needs:
      - resolve-config
      - validate-project
      - unity-tests
      - build-addressables
      - build-android
      - build-webgl
      - build-linux64
      - build-linuxserver
      - build-ios
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Write step summary
        shell: bash
        env:
          RUN_ID:        ${{ github.run_id }}
          COMMIT:        ${{ github.sha }}
          UNITY_VERSION: ${{ needs.resolve-config.outputs.unity-version }}
          PLATFORM:      ${{ inputs.platform }}
          ENVIRONMENT:   ${{ inputs.environment }}
          RUNNER_MODE:   ${{ inputs.runner-mode }}

          R_VALIDATE:         ${{ needs.validate-project.result }}
          R_TESTS:            ${{ needs.unity-tests.result }}
          R_ADDRESSABLES:     ${{ needs.build-addressables.result }}
          R_ANDROID:          ${{ needs.build-android.result }}
          R_WEBGL:            ${{ needs.build-webgl.result }}
          R_LINUX64:          ${{ needs.build-linux64.result }}
          R_LINUXSERVER:      ${{ needs.build-linuxserver.result }}
          R_IOS:              ${{ needs.build-ios.result }}

          ART_ANDROID:    unity-build-Android
          ART_WEBGL:      unity-build-WebGL
          ART_LINUX64:    unity-build-Linux64
          ART_LINUXSERVER: unity-build-LinuxServer
          ART_IOS:        unity-build-iOS
          ART_ADDR:       unity-build-Addressables
          ART_TESTS:      unity-tests-${{ inputs.test-mode }}
        run: |
          # Emoji helper
          emoji() {
            case "$1" in
              success)   echo "✅" ;;
              failure)   echo "❌" ;;
              skipped)   echo "⏭️" ;;
              cancelled) echo "🚫" ;;
              *)         echo "❓" ;;
            esac
          }

          {
            echo "## Unity Build — Final Report"
            echo ""
            echo "| Setting | Value |"
            echo "|---|---|"
            echo "| Run ID | \`${RUN_ID}\` |"
            echo "| Commit | \`${COMMIT:0:8}\` |"
            echo "| Unity version | \`${UNITY_VERSION:-unknown}\` |"
            echo "| Selected platform | \`${PLATFORM}\` |"
            echo "| Environment | \`${ENVIRONMENT}\` |"
            echo "| Runner mode | \`${RUNNER_MODE}\` |"
            echo ""
            echo "## Phase Results"
            echo ""
            echo "### Infrastructure"
            echo "| Job | Result |"
            echo "|---|---|"
            echo "| validate-project | $(emoji "${R_VALIDATE}") \`${R_VALIDATE}\` |"
            echo ""
            echo "### Tests"
            echo "| Job | Result | Artifact |"
            echo "|---|---|---|"
            echo "| unity-tests | $(emoji "${R_TESTS}") \`${R_TESTS}\` | \`${ART_TESTS}\` |"
            echo ""
            echo "### Addressables"
            echo "| Job | Result | Artifact |"
            echo "|---|---|---|"
            echo "| build-addressables | $(emoji "${R_ADDRESSABLES}") \`${R_ADDRESSABLES}\` | \`${ART_ADDR}\` |"
            echo ""
            echo "### Platform Builds"
            echo "| Platform | Result | Artifact |"
            echo "|---|---|---|"
            echo "| Android     | $(emoji "${R_ANDROID}") \`${R_ANDROID}\` | \`${ART_ANDROID}\` |"
            echo "| WebGL       | $(emoji "${R_WEBGL}") \`${R_WEBGL}\` | \`${ART_WEBGL}\` |"
            echo "| Linux64     | $(emoji "${R_LINUX64}") \`${R_LINUX64}\` | \`${ART_LINUX64}\` |"
            echo "| LinuxServer | $(emoji "${R_LINUXSERVER}") \`${R_LINUXSERVER}\` | \`${ART_LINUXSERVER}\` |"
            echo "| iOS         | $(emoji "${R_IOS}") \`${R_IOS}\` | \`${ART_IOS}\` |"
            echo ""
            echo "> Log artifacts: \`unity-build-<Platform>-logs\` (retained 14 days)"
          } >> "$GITHUB_STEP_SUMMARY"

          # Fail the report job if any core build failed
          FAILED=0
          for result in "${R_VALIDATE}" "${R_ANDROID}" "${R_WEBGL}" "${R_LINUX64}" "${R_LINUXSERVER}"; do
            [ "${result}" = "failure" ] && FAILED=1
          done
          # Tests failure is informational — does not block the report
          # iOS 'blocked' result is not a failure — reusable exits 0 with result=blocked
          if [ "${FAILED}" -eq 1 ]; then
            echo "::error::One or more build jobs failed. See individual job logs."
            exit 1
          fi
